(function() {
    'use strict';
    angular
        .module('sharedServices')
        .factory('utilities', utilities);

    function utilities(){
        var service = {
        };

        return service;
    }
}());
